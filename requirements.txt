# Web layer
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Storage + embeddings
qdrant-client>=1.10          # 1.10+ exposes query_points()
fastembed>=0.4               # ONNX sentence embeddings - no PyTorch
scikit-learn>=1.4            # TF-IDF fallback (EMBEDDING_BACKEND=tfidf)

# Code parsing. Python uses the stdlib ast module; these grammars cover
# JavaScript, JSX, TypeScript and TSX.
tree-sitter>=0.23
tree-sitter-javascript>=0.23
tree-sitter-typescript>=0.23

# Answer synthesis: any OpenAI-compatible endpoint (see README)
httpx>=0.27

# --- development ---
pytest>=8.0
